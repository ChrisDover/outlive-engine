# ── Database (Prisma / NeonDB — auth only) ────────────────────────────────────
POSTGRES_PRISMA_URL=postgresql://user:pass@host:5432/outlive_web

# ── NextAuth ──────────────────────────────────────────────────────
# REQUIRED — generate: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# ── Outlive Backend ───────────────────────────────────────────────
OUTLIVE_BACKEND_URL=http://localhost:8000
# REQUIRED — must match backend SERVICE_API_KEY. Generate: openssl rand -hex 32
OUTLIVE_SERVICE_KEY=

# ── Wearable OAuth ────────────────────────────────────────────────────────────
OURA_CLIENT_ID=
OURA_CLIENT_SECRET=
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=

# ── Email (Resend) ────────────────────────────────────────────────────────────
RESEND_API_KEY=
